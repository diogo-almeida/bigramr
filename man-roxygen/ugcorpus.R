#' @param unigram.corpus vector of bigrams that are associated with frequencies 
#'   in a bigram frequency database, like dataset \code{\link{unigram.frequencies}}.
#'
#' @param unigram.corpus.frequency vector of frequencies that are associated with
#'   bigrams in a bigram frequency database, like dataset 
#'   \code{\link{unigram.frequencies}}.
